# mysyncme プロジェクト

このリポジトリで作業する Claude Code 向けの案内ドキュメント。

## このプロジェクトは何か

**Gemini API を使った、キャラクター対話型 AI エージェントの WEB アプリ**。
ドコモの SyncMe のような「キャラクターと会話しながら、その裏でユーザーのパーソナライズデータが蓄積されていく」体験を、単一 HTML ファイルで実現することを目指す。

ユーザー想定: 自分の家族(子供 2 人)が中心。技術リテラシーのあるユーザーになら配布も可能(各自で API キーを入力する設計)。

## アーキテクチャの大方針(必ず守る)

1. **単一 HTML ファイル(`aiag.html`)で完結する** — サーバーは立てない。すべてブラウザ内で動く。
2. **画像だけ別ファイル**として `images/` 配下に置く(キャラクター画像)。これは例外。
3. **データは LocalStorage に保存** — IndexedDB やサーバー DB は使わない。
4. **Gemini API は各ユーザーが自分の API キーを入力する** — コードに API キーを埋め込まない(GitHub Public のため漏洩リスクあり)。
5. **iPhone Safari + ホーム画面追加(PWA 的利用)** が想定される主な利用シーン。
6. **外部ライブラリ・CDN は使わない** — HTML/CSS/JavaScript すべて 1 ファイルに含める。
7. **公開先**: GitHub Pages (`https://iteauauchocolat-maker.github.io/mysyncme/aiag.html`)

## ファイル構成

```
mysyncme/
├── CLAUDE.md              ← このファイル(全体方針)
├── README.md              ← リポジトリ概要
├── aiag.html              ← メインアプリ(本体)
├── images/                ← キャラクター画像
│   ├── miru_smile.png
│   ├── miru_surprise.png
│   ├── miru_munch.png
│   ├── pui_munch.png
│   ├── pui_sleepy.png
│   └── pui_surprise.png
└── docs/
    ├── decisions.md       ← 設計判断の経緯
    ├── roadmap.md         ← 今後の開発予定
    └── characters.md      ← キャラクター設定の詳細
```

## 技術スタック

- HTML / CSS / Vanilla JavaScript(フレームワーク・ビルドツール不使用)
- Gemini API(`gemini-2.5-flash` がデフォルト、`gemini-2.5-pro` も選択可)
- LocalStorage でデータ永続化

## 現在の機能(完成済み)

- 4 タブ切替: チャット / プロフィール(準備中) / スケジュール(準備中) / 設定
- キャラクター 2 人選択(ミル / プイ)
- キャラごとのチャット履歴(分離管理)
- キャラごとの口調・性格(システムプロンプトで制御)
- 表情切替(AI が `[[EMOTION:xxx]]` タグを返す → 画像差替)
- キャラごとのテーマカラー(背景・吹き出し色が変化)
- ライト / ダークモード自動切替
- iPhone Safari 対応(セーフエリア・ホーム画面追加)

## 未実装(これから作る)

優先順は `docs/roadmap.md` 参照。

## ユーザーについて

- プログラミングや開発の専門知識はほぼ無い
- 専門用語は最低限に。指示は具体的に。
- このプロジェクトは Claude(チャット版)との対話で要件定義され、claude.ai で初版生成 → GitHub Web UI で公開、という流れで来ている。Claude Code への切替はこのドキュメント作成のタイミング。
- 過去の経緯は `docs/decisions.md` を参照。

## Claude Code 作業時のお約束

### 作業フロー
- main ブランチに直接コミット&プッシュで運用する(PR ベースではない)
- 大きな変更や破壊的変更を加える前は、ユーザーに確認を取る
- コミットメッセージは日本語で簡潔に(例: `ミルの口調を調整`、`スケジュール画面を追加`)

### コードを書く・直すとき
- **既存のスタイル(命名・コメント・フォーマット)を維持**する。一貫性が読みやすさを生む。
- **JavaScript は ES2020 程度の素朴な構文**を使う。トランスパイル前提の機能は避ける。
- **iPhone Safari の挙動を最優先**で考える。`100dvh`、`env(safe-area-inset-*)`、`-webkit-` プレフィックスを忘れずに。
- **API キーや個人情報をログに残さない**。`console.log` でも避ける。
- **LocalStorage のキー名**は `STORAGE_KEYS` 定数経由で参照する。直書き禁止。
- **ファイルが極端に大きくなったら警告**する。GitHub Pages の gzip 圧縮で実用上は問題になりにくい(99KB でも転送時 25KB 程度)ので、神経質にならなくて良い(目安: 200KB を超えそうなら相談。それまでは普通に機能追加して OK)。

### キャラクター機能の拡張時
- キャラクターは将来増える可能性がある。`CHARACTERS` 配列に追加するだけで済むよう、ハードコードは避ける。
- 新キャラ追加時は、画像 6 枚(各表情)+ システムプロンプト + テーマカラー設定が必要。詳細は `docs/characters.md`。

### Gemini API 利用時
- ユーザーの入力(チャット内容)はあくまでブラウザ内で完結。Gemini への送信以外で外部送信しない。
- API エラーは握りつぶさず、ユーザーに分かりやすく表示する(現在は赤バブルでチャット内表示)。
- レート制限・コスト配慮: 不要な API 呼び出しを避ける。

### コミット前の確認
- ブラウザ(PC + できれば iPhone)で動作確認してから push する
- JavaScript の構文エラーがないか確認(壊れると画面真っ白になる)

## 困ったとき

- 設計判断の理由が分からない場合: `docs/decisions.md` を参照
- 次に何を作るべきか分からない場合: `docs/roadmap.md` を参照
- キャラクター追加・調整: `docs/characters.md` を参照
- それでも分からない場合: ユーザーに質問する(勝手に大きな判断をしない)
